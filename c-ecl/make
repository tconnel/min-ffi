#!/bin/bash
fullpath=$(realpath $(dirname $0))
cd $fullpath
[[ -x clean ]] && ./clean
mkdir -p bin obj lib
ecl -o obj/hello-world.o -s -compile hello-world.lisp >/dev/null
ecl -o lib/hello-world.fas -compile hello-world.lisp >/dev/null
